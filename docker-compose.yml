services:
  spring-mvc:
    build: .
    container_name: spring-mvc
    env_file: .env
    ports:
      - "${SPRING_PORT}:8081"
    # For Linux hosts, this ensures host.docker.internal works:
    extra_hosts:
      - "host.docker.internal:host-gateway"